const app=document.getElementById("app");let currentUser=null;
const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));
async function api(url,opt={}){const r=await fetch(url,{headers:{"Content-Type":"application/json"},...opt});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||"Request failed");return d}
function loginView(){app.innerHTML=`<div class="login-wrap"><div class="login-card"><div class="brand">☀ Solar CRM</div><p class="muted">V2 • Secure lead management</p><div id="login-msg"></div><form id="login-form"><div class="field"><label>Login ID</label><input id="username" autocomplete="username" required></div><div class="field"><label>Password</label><input id="password" type="password" autocomplete="current-password" required></div><button class="btn btn-primary" style="width:100%">Login</button></form></div></div>`;
document.getElementById("login-form").onsubmit=async e=>{e.preventDefault();try{const d=await api("/api/login",{method:"POST",body:JSON.stringify({username:username.value,password:password.value})});currentUser=d.user;renderShell()}catch(x){document.getElementById("login-msg").innerHTML=`<div class="notice error">${esc(x.message)}</div>`}}}
function renderShell(){let nav=`<button data-page="dashboard" class="active">Dashboard</button>`;if(currentUser.role==="agent")nav+=`<button data-page="create">＋ Create Lead</button><button data-page="myleads">My Leads</button>`;if(currentUser.role==="qc_manager")nav+=`<button data-page="qcleads">QC Queue</button>`;if(currentUser.role==="admin")nav+=`<button data-page="adminleads">All Leads</button><button data-page="users">Users</button>`;
app.innerHTML=`<div class="shell"><aside class="sidebar"><div class="logo">☀ Solar CRM <span class="role">V2</span></div><div class="nav">${nav}</div><div class="userbox"><strong>${esc(currentUser.displayName)}</strong><br><span class="role">${esc(currentUser.role.replace("_"," "))}</span><br><br><button id="logout" class="btn btn-secondary">Logout</button></div></aside><main class="main"><div id="page"></div></main></div>`;
document.querySelectorAll(".nav button").forEach(b=>b.onclick=()=>{document.querySelectorAll(".nav button").forEach(x=>x.classList.remove("active"));b.classList.add("active");route(b.dataset.page)});document.getElementById("logout").onclick=async()=>{await api("/api/logout",{method:"POST"});currentUser=null;loginView()};route("dashboard")}
function title(t,s){return `<div class="top"><div><div class="title">${t}</div><div class="muted">${s||""}</div></div></div>`}
function statusPill(s){let c=s==="Qualified"?"good":s==="Unqualified"?"bad":"pending";return `<span class="pill ${c}">${esc(s)}</span>`}
async function route(p){if(p==="dashboard")return dashboard();if(p==="create")return createLead();if(p==="myleads")return myLeads();if(p==="qcleads")return qcLeads();if(p==="adminleads")return adminLeads();if(p==="users")return users()}
async function dashboard(){let el=document.getElementById("page");
if(currentUser.role==="admin"){let d=await api("/api/admin/stats");el.innerHTML=title("Admin Dashboard","Full CRM overview")+stats([{a:"Total Leads",n:d.total},{a:"Appointments",n:d.appointments},{a:"Callbacks",n:d.callbacks},{a:"Pending QC",n:d.pending},{a:"Qualified",n:d.qualified},{a:"Unqualified",n:d.unqualified},{a:"No Solar",n:d.noSolar},{a:"Has Solar",n:d.hasSolar}])+`<br><div class="card"><h3>Agent performance</h3>${agentTable(d.agents)}</div>`}
else if(currentUser.role==="qc_manager"){let d=await api("/api/qc/stats");el.innerHTML=title("QC Manager Dashboard","Review leads and record the QC decision")+stats([{a:"Total Leads",n:d.total},{a:"Pending QC",n:d.pending},{a:"Qualified",n:d.qualified},{a:"Unqualified",n:d.unqualified}])+`<br><div class="card"><h3>QC rule</h3><div class="muted">Review the lead, add a QC Comment, then choose Qualified or Unqualified. Customer mobile numbers are not available to QC.</div></div>`}
else {let d=await api("/api/my-leads");el.innerHTML=title("Agent Dashboard",`Welcome, ${esc(currentUser.displayName)}`)+stats([{a:"My Leads",n:d.leads.length},{a:"Appointments",n:d.leads.filter(x=>x.product_type==="Appointment").length},{a:"Callbacks",n:d.leads.filter(x=>x.product_type==="Callback").length},{a:"Qualified",n:d.leads.filter(x=>x.status==="Qualified").length}])+`<br><div class="card"><h3>Mobile number protection</h3><div class="muted">Mobile numbers are accepted during lead creation but are never returned to your account after submission. Only Admin can retrieve them.</div></div>`}}
function stats(a){return `<div class="grid">${a.map(x=>`<div class="card"><div class="muted">${x.a}</div><div class="stat">${x.n}</div></div>`).join("")}</div>`}
function agentTable(a){if(!a.length)return `<div class="muted">No agents yet.</div>`;return `<div class="table-wrap"><table class="table"><tr><th>Agent</th><th>ID</th><th>Leads</th><th>Qualified</th><th>Unqualified</th></tr>${a.map(x=>`<tr><td>${esc(x.display_name)}</td><td>${esc(x.username)}</td><td>${x.lead_count}</td><td>${x.qualified}</td><td>${x.unqualified}</td></tr>`).join("")}</table></div>`}
function select(n,l,opts,req=true){return `<div class="field"><label>${l}${req?" *":""}</label><select name="${n}" ${req?"required":""}><option value="">Select...</option>${opts.map(x=>`<option>${esc(x)}</option>`).join("")}</select></div>`}
function createLead(){document.getElementById("page").innerHTML=title("Create New Lead","The mobile number becomes inaccessible to Agents after saving.")+`<div class="card form-card"><div id="lead-msg"></div><form id="lead-form"><div class="section-title">Lead Type</div><div class="form-grid">${select("product_type","Product Type",["Callback","Appointment"])}${select("solar_status","Solar Status",["No Solar","Has Solar"])}</div><div class="section-title">Customer Details</div><div class="form-grid"><div class="field"><label>First Name *</label><input name="first_name" required></div><div class="field"><label>Last Name *</label><input name="last_name" required></div><div class="field"><label>Mobile Number *</label><input name="mobile_number" inputmode="tel" required></div><div class="field"><label>Email</label><input name="email" type="email"></div><div class="field"><label>Street Name *</label><input name="street_name" required></div><div class="field"><label>Suburb *</label><input name="suburb" required></div><div class="field"><label>Postcode *</label><input name="postcode" maxlength="4" required></div>${select("free_standing","Free Standing",["Yes","No"])}${select("roof_type","Roof Type",["Tile","Metal","Colorbond","Terracotta","Other"])}<div class="field"><label>Bill *</label><input name="bill" placeholder="$300" required></div>${select("under_75","Under 75",["Yes","No"])}</div><div id="solar-extra" class="hidden"><div class="section-title">Existing Solar Details</div><div class="form-grid"><div class="field"><label>Number of Panels *</label><input name="number_of_panels" type="number" min="0"></div><div class="field"><label>Panels Age *</label><input name="panels_age"></div>${select("battery","Battery",["Yes","No"])}</div></div><div class="section-title">Qualification & Booking</div><div class="form-grid">${select("sunlight","Sunlight",["Good","Average","Poor","Unknown"])}${select("quote","Quote",["Yes","No","Not Yet"])}<div class="field"><label>Callback / Appointment Date *</label><input name="appointment_date" type="date" required></div><div class="field"><label>Callback / Appointment Time *</label><input name="appointment_time" type="time" required></div><div class="field full"><label>Additional Comments</label><textarea name="additional_comment"></textarea></div></div><button class="btn btn-primary">Create Lead</button></form></div>`;
const f=document.getElementById("lead-form"), solar=f.querySelector('[name="solar_status"]'), extra=document.getElementById("solar-extra");function toggle(){extra.classList.toggle("hidden",solar.value!=="Has Solar");extra.querySelectorAll("input,select").forEach(x=>x.required=solar.value==="Has Solar")}solar.onchange=toggle;f.onsubmit=async e=>{e.preventDefault();try{let d=await api("/api/leads",{method:"POST",body:JSON.stringify(Object.fromEntries(new FormData(f).entries()))});document.getElementById("lead-msg").innerHTML=`<div class="notice success">Lead created: <strong>${esc(d.leadCode)}</strong>. Mobile number is now protected.</div>`;f.reset();toggle()}catch(x){document.getElementById("lead-msg").innerHTML=`<div class="notice error">${esc(x.message)}</div>`}}}
function basicTable(rows,admin=false,qc=false){if(!rows.length)return `<div class="card empty muted">No leads found.</div>`;return `<div class="table-wrap"><table class="table"><tr><th>Lead ID</th>${admin||qc?"<th>Agent</th>":""}<th>Customer</th>${admin?"<th>Mobile</th>":""}<th>Solar</th><th>Type</th><th>Suburb</th><th>Postcode</th><th>Date</th><th>Time</th><th>Status</th>${qc?"<th>QC Comment</th><th>Action</th>":admin?"<th>QC Comment</th>":""}</tr>${rows.map(r=>`<tr><td>${esc(r.lead_code)}</td>${admin||qc?`<td>${esc(r.agent_name)}<br><span class="muted">${esc(r.agent_username)}</span></td>`:""}<td>${esc(r.first_name)} ${esc(r.last_name)}</td>${admin?`<td><strong>${esc(r.mobile_number)}</strong></td>`:""}<td>${esc(r.solar_status)}</td><td>${esc(r.product_type)}</td><td>${esc(r.suburb)}</td><td>${esc(r.postcode)}</td><td>${esc(r.appointment_date)}</td><td>${esc(r.appointment_time)}</td><td>${statusPill(r.status)}</td>${qc?`<td>${esc(r.qc_comment||"—")}</td><td>${r.status==="Pending QC"?`<button class="btn btn-success mini" onclick="reviewLead(${r.id},'Qualified')">Qualified</button> <button class="btn btn-danger mini" onclick="reviewLead(${r.id},'Unqualified')">Unqualified</button>`:"Reviewed"}</td>`:admin?`<td>${esc(r.qc_comment||"—")}</td>`:""}</tr>`).join("")}</table></div>`}
async function myLeads(){let d=await api("/api/my-leads");document.getElementById("page").innerHTML=title("My Leads","Only your leads. Mobile numbers are excluded.")+basicTable(d.leads)}
async function qcLeads(){let d=await api("/api/qc/leads");document.getElementById("page").innerHTML=title("QC Queue","Mobile numbers are deliberately unavailable to QC Managers")+`<div class="toolbar"><select id="qcfilter"><option value="">All</option><option>Pending QC</option><option>Qualified</option><option>Unqualified</option></select></div><div id="qc-table">${basicTable(d.leads,false,true)}</div>`;qcfilter.onchange=async()=>{let x=await api("/api/qc/leads?status="+encodeURIComponent(qcfilter.value));document.getElementById("qc-table").innerHTML=basicTable(x.leads,false,true)}}
async function reviewLead(id,status){let comment=prompt(`Enter QC Comment for ${status}:`);if(comment===null)return;if(!comment.trim())return alert("QC Comment is required.");try{await api("/api/qc/leads/"+id+"/review",{method:"PATCH",body:JSON.stringify({status,qc_comment:comment})});qcLeads()}catch(e){alert(e.message)}}
async function adminLeads(){let d=await api("/api/admin/leads");document.getElementById("page").innerHTML=title("All Leads","Admin-only: full lead data including protected mobile numbers")+`<div class="toolbar"><input id="search" placeholder="Search lead, name, suburb, agent or mobile"><select id="af"><option value="">All statuses</option><option>Pending QC</option><option>Qualified</option><option>Unqualified</option></select><button class="btn btn-secondary" onclick="loadAdmin()">Search</button><a class="btn btn-primary" href="/api/admin/export">Export CSV</a></div><div id="admin-table">${basicTable(d.leads,true)}</div>`}
async function loadAdmin(){let q=encodeURIComponent(document.getElementById("search").value),s=encodeURIComponent(document.getElementById("af").value);let d=await api("/api/admin/leads?q="+q+"&status="+s);document.getElementById("admin-table").innerHTML=basicTable(d.leads,true)}
async function users(){let d=await api("/api/admin/users");document.getElementById("page").innerHTML=title("User Management","Create, deactivate or reset accounts")+`<div class="two-col"><div class="card"><h3>Create User</h3><div id="user-msg"></div><form id="user-form"><div class="field"><label>Display Name *</label><input name="display_name" required></div><div class="field"><label>Login ID *</label><input name="username" required></div>${select("role","Role",["agent","qc_manager","admin"])}<div class="field"><label>Password *</label><input name="password" type="password" minlength="8" required></div><button class="btn btn-primary">Create User</button></form></div><div class="card"><h3>Roles</h3><div class="muted"><b>Agent:</b> creates and sees own leads only.<br><br><b>QC Manager:</b> reviews leads and records Qualified/Unqualified + QC Comment.<br><br><b>Admin:</b> full control and mobile-number access.</div></div></div><br><div class="table-wrap"><table class="table"><tr><th>Name</th><th>Login ID</th><th>Role</th><th>Active</th><th>Created</th><th>Actions</th></tr>${d.users.map(u=>`<tr><td>${esc(u.display_name)}</td><td>${esc(u.username)}</td><td>${esc(u.role)}</td><td>${u.active?"Yes":"No"}</td><td>${esc(u.created_at)}</td><td class="actions">${u.id!==currentUser.id?`<button class="btn ${u.active?"btn-danger":"btn-success"} mini" onclick="toggleUser(${u.id},${!u.active})">${u.active?"Deactivate":"Activate"}</button><button class="btn btn-secondary mini" onclick="resetPw(${u.id})">Reset Password</button>`:"Current account"}</td></tr>`).join("")}</table></div>`;
document.getElementById("user-form").onsubmit=async e=>{e.preventDefault();try{await api("/api/admin/users",{method:"POST",body:JSON.stringify(Object.fromEntries(new FormData(e.target).entries()))});users()}catch(x){document.getElementById("user-msg").innerHTML=`<div class="notice error">${esc(x.message)}</div>`}}}
async function toggleUser(id,active){try{await api("/api/admin/users/"+id+"/active",{method:"PATCH",body:JSON.stringify({active})});users()}catch(e){alert(e.message)}}
async function resetPw(id){let p=prompt("Enter new password (minimum 8 characters):");if(p===null)return;try{await api("/api/admin/users/"+id+"/password",{method:"PATCH",body:JSON.stringify({password:p})});alert("Password reset successfully.")}catch(e){alert(e.message)}}
(async()=>{try{let d=await api("/api/me");if(d.user){currentUser=d.user;renderShell()}else loginView()}catch(e){loginView()}})();